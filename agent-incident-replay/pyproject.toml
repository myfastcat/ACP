[project]
name = "agent-incident-replay"
version = "0.1.0"
description = "Turn agent incident traces into portable replay regression evidence"
requires-python = ">=3.10"

[project.scripts]
air = "agent_incident_replay.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
