# Next milestone

Implement a true Level 1 zero-code adapter for one concrete framework. It must consume that framework's native observable trace/event mechanism without ACP imports in application code, generate/locate the tool-call artifact in CI, and pass the zero-code adapter definition-of-done tests in `docs/ZERO_CODE_INTEGRATION.md`.
